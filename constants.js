export const DB_NAME = "Marvellous_Form"

// const existedFormByTitle = await Form.findOne( { title } );
//   try {
//     if (existedFormByTitle) {
//       throw new ApiError(400, "Form with this title already exists.");
//     }
//   } catch (err) {
//     throw new ApiError(404, "Server Error!");
//   }